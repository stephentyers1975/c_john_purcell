// #pragma once // prevents header file being included more than once - non standard
#ifndef _FUNCS_ // supported way to define below once
#define _FUNCS_

struct Value {
    int id;
};

void greet();

#endif
